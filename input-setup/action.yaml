name: Input setup
author: trunk.io
description: Sets up INPUT_ env vars and masks sensitive fields in the CheckService payload

inputs:
  use-payload:
    description: Whether or not the setup should use the payload
    required: false
    default: ""

# runs:
#   using: composite
#   steps:
#     - name: test
#       shell: bash
#       run: |
#         echo "========="
#         find / | grep /mask.js || true
#         echo "========="
#         node `find / | grep /mask.js`

runs:
  using: node16
  main: ${{ inputs.good-path }}/input-setup/mask.js
